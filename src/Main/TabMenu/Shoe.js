import * as React from 'react';
import { View, Text } from 'react-native';

function Shoe() {
    return (
        <View style={{
            flex: 1,
            justifyContent: 'center',
            alignItems: 'center'
        }}>
            <Text>Shoe화면</Text>
        </View>
    );
}

export default Shoe;