import * as React from 'react';
import { View, Text } from 'react-native';
function ItemInfo() {
    return (
        <View style={{
            flex: 1,
            justifyContent: 'center',
            alignItems: 'center'
        }}>
            <Text>ItemInfo화면</Text>
        </View>
    );
}

export default ItemInfo;