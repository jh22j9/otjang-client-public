import * as React from 'react';
import { View, Text } from 'react-native';
function Setting() {
    return (
        <View style={{
            flex: 1,
            justifyContent: 'center',
            alignItems: 'center'
        }}>
            <Text>Statistics화면</Text>
        </View>
    );
}

export default Setting;